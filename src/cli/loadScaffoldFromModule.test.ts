import { scaffold } from "./load";

describe(scaffold, () => {
  test('', async () => {
    const fn = jest.fn()
    await scaffold(fn)
    // TODO:
  })

  test('', async () => {
    const file = 'test'
    await scaffold(file)
    // TODO:
  })
})